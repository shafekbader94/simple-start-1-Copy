apiVersion: v1
kind: Service
metadata:
  name: nestjs-service
  labels:
    app: nestjs
spec:
  type: NodePort
  selector:
    app: nestjs
  ports:
  - port: 80         # external port
    targetPort: 3000 # container port
    nodePort: 31597  # access via localhost:31597