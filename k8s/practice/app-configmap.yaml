# ====================
# Practice: ConfigMap
# ====================
# Configuration for the practice app

apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_NAME: "Practice App"
  APP_VERSION: "1.0"
  DB_HOST: "localhost"
  DB_PORT: "3306"
  CACHE_ENABLED: "true"
